SuccessClass{
	
	/* Declaracao variaveis, usando tipos e
	 reconhecimento de comentários multiline
	 int
	 float
	 string
	 char
	 boolean
	*/
	int inteiroGlobal = 1.1;
	string stringGlobal = "\n";
	char caracterGlobal = c;
	boolean booleanoGlobal = boolean;
	float floatGlobal = .00000000001;
	float floatGlobal1 = 10.;
	float g = @+1;
'a'
a
@
"1.;"
.1
"\"
"\n"
"\r"

	/ Construtor com XOR,NOT,AND,OR e comentario singleline
	(boolean paramBool, int paramInt)
		Success success = new 
		super(,paramBool);
		boolean outroBoleano = NOT OR paramBool OR NOT(paramInt == globalInt);
		boolean resultBoolean = 1 xor 2 not 2 and 4 or 5;
	}
	
	public int init  {
		int i = -1;
		string s = "string\n"
		float floatInterno = -2.10;
		
		return i + string;
	}
	
	public int funcao (){
		
		for(int i = 0, i<5, i++) {
			if(intGlobal <= i){
				if(intGlobal != i){
					i = i*'a';
					i = "a"/1;
					i = i%2;
					int const = null;
					i--;
				}
				break;
			} else {
				i++;
			}
		}
		byte teclado = (byte) read();
		System.out.print("oi");
		
		
		return i;
	}
	
	
}
